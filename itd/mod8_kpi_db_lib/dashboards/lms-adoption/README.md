# Lms Adoption Dashboard

Open `index.html` through GitHub Pages or another web server. Edit `data.md` to update the displayed values. If you open the HTML directly from disk, the page uses embedded fallback sample data because many browsers block local `fetch()` calls.

All Ardent ISD values are illustrative sample data.
